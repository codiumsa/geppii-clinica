<div class="modal fade" id="procesoDelete">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" {{action "cancel"}}  data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title">Eliminar Proceso</h4>
      </div>
      <div class="modal-body">
        {{#if isCancelado}}
          <p>Desea eliminar definitivamente el proceso?</p>
          <p>Obs: Si existen registros de producción relacionadas a este proceso de producción no se podrá eliminar el regsitro. </p>
        {{else}}
          <p>Desea cancelar el proceso?</p>
          <p>Obs: Ya no podrá registrar producciones de este proceso.</p>
        {{/if}}
        
      </div>
      <div class="modal-footer">
        <button type="button" {{action "deleteRecord"}} onkeydown="Bodega.ignoreEscapeDeleteModalWindow('controller:proceso.delete', 'procesos');" class="btn btn-theme" data-dismiss="modal" autofocus>Si</button>
        <button type="button" {{action "cancel"}} class="btn btn-default" data-dismiss="modal">No</button>
      </div>
    </div>
  </div>
</div>
